import Link from "next/link";
import { updatePassword } from "../actions";
type Props={searchParams:Promise<{error?:string}>};
export default async function UpdatePasswordPage({searchParams}:Props){const message=await searchParams;return <main className="auth-shell compact-auth"><section className="auth-story"><div className="brand light">Smart<span>One</span></div><h1>یک رمز تازه و امن بسازید</h1><p>از رمز منحصربه‌فرد حداقل هشت‌کاراکتری استفاده کنید و آن را در اختیار دیگران قرار ندهید.</p></section><section className="auth-card"><p className="eyebrow">امنیت حساب</p><h2>تعیین رمز جدید</h2><p className="muted">رمز جدید را دوبار وارد کنید.</p>{message.error?<p className="form-message error">{message.error}</p>:null}<form action={updatePassword}><label>رمز عبور جدید<input name="password" type="password" dir="ltr" minLength={8} required autoComplete="new-password"/></label><label>تکرار رمز عبور<input name="confirmPassword" type="password" dir="ltr" minLength={8} required autoComplete="new-password"/></label><button className="primary wide">ذخیره رمز جدید</button></form><Link className="auth-back" href="/auth">بازگشت به ورود</Link></section></main>}
