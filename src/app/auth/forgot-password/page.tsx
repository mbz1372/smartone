import Link from "next/link";
import { requestPasswordReset } from "../actions";
type Props={searchParams:Promise<{error?:string;success?:string}>};
export default async function ForgotPasswordPage({searchParams}:Props){const message=await searchParams;return <main className="auth-shell compact-auth"><section className="auth-story"><div className="brand light">Smart<span>One</span></div><h1>دسترسی امن به فضای کاری</h1><p>لینک بازیابی فقط برای ایمیل ثبت‌شده ارسال می‌شود و بعد از استفاده منقضی خواهد شد.</p></section><section className="auth-card"><p className="eyebrow">بازیابی حساب</p><h2>فراموشی رمز عبور</h2><p className="muted">ایمیل حساب خود را وارد کنید تا لینک تعیین رمز جدید ارسال شود.</p>{message.error?<p className="form-message error">{message.error}</p>:null}{message.success?<p className="form-message success">{message.success}</p>:null}<form action={requestPasswordReset}><label>ایمیل<input name="email" type="email" dir="ltr" required autoComplete="email" placeholder="you@company.com"/></label><button className="primary wide">ارسال لینک بازیابی</button></form><Link className="auth-back" href="/auth">بازگشت به صفحه ورود</Link></section></main>}
